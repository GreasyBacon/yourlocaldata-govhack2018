var showModal = function() {
  document.getElementById('community_stats').classList.add('is-active');
}

var hideModal = function() {
  document.getElementById('community_stats').classList.remove('is-active');	
}